---
description: Vygeneruje z tokens.json samostatný HTML styleguide design systému
argument-hint: "[tokens.json] [--out <soubor>]"
---

Vygeneruj demo design systému pomocí skillu `design-system-demo`.

Argumenty: $ARGUMENTS

Postup je v SKILL.md — najdi `tokens.json`, zavolej `pnpm design-demo`,
a vrať cestu k výsledku jako klikatelný `file://` odkaz. HTML nesestavuj sám.
